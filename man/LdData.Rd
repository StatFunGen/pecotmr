% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LdData.R
\name{LdData}
\alias{LdData}
\title{Create an LdData Object}
\usage{
LdData(
  correlation = NULL,
  genotypeHandle = NULL,
  snpIdx = NULL,
  variants,
  blockMetadata,
  nRef = 0L,
  mixtureWeights = NULL
)
}
\arguments{
\item{correlation}{A correlation matrix, list of matrices, or NULL.}

\item{genotypeHandle}{A genotype panel (see
\code{\link{readGenotypes}}), a list of panels for a mixture reference,
a matrix of already-extracted dosages, or NULL.}

\item{snpIdx}{Vector of 1-based SNP indices, coerced to integer, or NULL.}

\item{variants}{A GRanges with variant metadata (must have variant_id in
mcols, plus A1, A2).}

\item{blockMetadata}{GRanges of blocks, or data.frame with block info.}

\item{nRef}{Integer, reference panel sample size.}

\item{mixtureWeights}{Optional numeric vector of mixing proportions, one per
panel in \code{genotypeHandle} when it is a list. Must be non-negative and
sum to 1. Required whenever \code{genotypeHandle} is a list and downstream
code will call \code{getCorrelation()}.}
}
\value{
An \code{LdData} object.
}
\description{
Construct an \code{LdData} from a correlation matrix and/or
  genotype handle, plus variant metadata as a GRanges.
}
\examples{
data(eqtlRegionExample)
X <- eqtlRegionExample$X[, 1:8]
gr <- GenomicRanges::GRanges("22",
  IRanges::IRanges(seq(1L, by = 100L, length.out = 8), width = 1L))
ld <- LdData(correlation = cor(X), variants = gr,
  blockMetadata = S4Vectors::DataFrame(
    chrom = "22", start = 1L, end = 1000L))
ld
}
