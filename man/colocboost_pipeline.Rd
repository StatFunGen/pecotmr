% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colocboost_pipeline.R
\name{colocboost_pipeline}
\alias{colocboost_pipeline}
\title{Multi-trait colocalization analysis protocol pipeline}
\usage{
colocboost_pipeline(
  region_data,
  focal_trait = NULL,
  event_filters = NULL,
  xqtl_coloc = TRUE,
  joint_gwas = FALSE,
  separate_gwas = FALSE,
  maf_cutoff = 5e-04,
  pip_cutoff_to_skip_ind = 0,
  keep_indel = TRUE,
  pip_cutoff_to_skip_sumstat = 0,
  qc_method = NULL,
  impute = TRUE,
  impute_opts = list(rcond = 0.01, R2_threshold = 0.6, minimum_ld = 5, lamb = 0.01),
  ...
)
}
\arguments{
\item{region_data}{A region data loaded from \code{load_regional_data}.}

\item{focal_trait}{Name of trait if perform focaled ColocBoost}

\item{event_filters}{A list of pattern for filtering events based on context names. Example: for sQTL, list(type_pattern = ".*clu_(\\d+_[+-?]).*",valid_pattern = "clu_(\\d+_[+-?]):PR:",exclude_pattern = "clu_(\\d+_[+-?]):IN:")}

\item{maf_cutoff}{A scalar to remove variants with maf < maf_cutoff, dafault is 0.005.}

\item{pip_cutoff_to_skip_ind}{A vector of cutoff values for skipping analysis based on PIP values for each context. Default is 0.}

\item{pip_cutoff_to_skip_sumstat}{A vector of cutoff values for skipping analysis based on PIP values for each sumstat Default is 0.}

\item{qc_method}{Quality control method to use. Options are "none", "rss_qc", "slalom", or "dentist". \code{NULL} and \code{"rss_qc"} are treated as \code{"none"} for basic-only summary-stat preprocessing.}

\item{impute}{Logical; if TRUE, performs imputation for outliers identified in the analysis (default: TRUE).}

\item{impute_opts}{A list of imputation options including rcond, R2_threshold, and minimum_ld (default: list(rcond = 0.01, R2_threshold = 0.6, minimum_ld = 5)).}

\item{...}{Additional arguments passed to the ColocBoost analysis calls.}
}
\value{
A list containing \code{xqtl_coloc}, \code{joint_gwas}, \code{separate_gwas},
and \code{computing_time}. Analyses that are not requested or cannot be run
return \code{NULL} in their corresponding result slot.
}
\description{
This function performs protocol-level multi-trait colocalization using
ColocBoost. It accepts loaded regional data, performs QC once, then runs the
requested xQTL-only, joint GWAS, and separate GWAS analyses.
}
