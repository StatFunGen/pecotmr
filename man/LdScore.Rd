% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LdScore.R
\name{LdScore}
\alias{LdScore}
\title{Create an LdScore}
\usage{
LdScore(
  snpInfo,
  ldScores,
  ldScoreWeights,
  ldBlocks,
  nRef,
  inSample = FALSE,
  genome = NA_character_,
  ldMatrixList = list()
)
}
\arguments{
\item{snpInfo}{A \code{data.frame} with columns \code{SNP}, \code{CHR},
\code{BP}, \code{A1}, \code{A2} (and optionally \code{MAF}).}

\item{ldScores}{A numeric matrix, one row per variant. The first column is
the base LD score (sum of r^2); further columns are
annotation-stratified scores.}

\item{ldScoreWeights}{Numeric regression weights, one per variant.}

\item{ldBlocks}{A \code{GRanges} of LD block intervals.}

\item{nRef}{Integer, sample size of the LD reference panel.}

\item{inSample}{Logical, whether the reference is the GWAS cohort.}

\item{genome}{Character, genome build; recorded in \code{seqinfo()}.}

\item{ldMatrixList}{Optional list of per-block LD matrices (g-LDSC only).}
}
\value{
An \code{LdScore}.
}
\description{
Bundle pre-computed LD scores with the variants they describe.
}
\examples{
snpInfo <- data.frame(SNP = paste0("rs", 1:4), CHR = "chr1",
  BP = c(50L, 150L, 250L, 350L), A1 = "A", A2 = "G")
blocks <- GenomicRanges::GRanges("chr1",
  IRanges::IRanges(c(1L, 200L), c(199L, 400L)))
ls <- LdScore(snpInfo = snpInfo,
  ldScores = matrix(runif(4), ncol = 1, dimnames = list(NULL, "base_l2")),
  ldScoreWeights = rep(1, 4), ldBlocks = blocks, nRef = 100L,
  inSample = FALSE, genome = "hg19")
length(ls)
head(getLdScores(ls))
}
