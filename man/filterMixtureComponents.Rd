% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mashWrapper.R
\name{filterMixtureComponents}
\alias{filterMixtureComponents}
\title{Filter conditions from mash prior mixture components}
\usage{
filterMixtureComponents(conditionsToKeep, U, w = NULL, wCutoff = 1e-04)
}
\arguments{
\item{conditionsToKeep}{Character vector of condition names to retain.}

\item{U}{Named list of prior covariance matrices (one per mixture component).}

\item{w}{Optional numeric vector of mixture weights aligned to \code{U}.}

\item{wCutoff}{Numeric. Drop components with weight below this. Default
\code{1e-4}.}
}
\value{
A list with the filtered \code{U} (and \code{w} when supplied).
}
\description{
Drop the conditions not in \code{conditionsToKeep} from each prior covariance
matrix in \code{U}, optionally removing components whose weight is below
\code{wCutoff}.
}
\examples{
conditionsToKeep <- c("cond1", "cond2")
cn <- c("cond1", "cond2", "cond3")
U <- list(shared = diag(3), corr = matrix(0.3, 3, 3) + diag(0.7, 3))
U <- lapply(U, function(m) {
  dimnames(m) <- list(cn, cn)
  m
})
filterMixtureComponents(conditionsToKeep = conditionsToKeep, U = U)
}
