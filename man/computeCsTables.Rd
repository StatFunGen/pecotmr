% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fineMappingWrappers.R
\name{computeCsTables}
\alias{computeCsTables}
\title{Compute Credible-Set Tables From a Fine-Mapping Fit}
\usage{
computeCsTables(
  fit,
  dataX,
  coverage = NULL,
  secondaryCoverage = c(0.7, 0.5),
  method = "susie",
  csInput = c("X", "Xcorr", "fsusie"),
  minAbsCorr = 0.8,
  medianAbsCorr = NULL
)
}
\arguments{
\item{fit}{A SuSiE-family fit (e.g. from \code{susieR::susie}) carrying
\code{sets}, \code{pip}, and (for fSuSiE) LBF fields.}

\item{dataX}{Numeric genotype / design matrix (samples x variants) the fit
was computed on; used to assess credible-set purity.}

\item{coverage}{Numeric primary coverage level, or \code{NULL} to use the
fit's requested coverage (falling back to \code{0.95}).}

\item{secondaryCoverage}{Numeric vector of additional coverage levels.}

\item{method}{Character method token (e.g. \code{"susie"}, \code{"fsusie"}).}

\item{csInput}{One of \code{"X"}, \code{"Xcorr"}, \code{"fsusie"}: how
credible-set purity is computed.}

\item{minAbsCorr, medianAbsCorr}{Purity thresholds (minimum and median
absolute correlation).}
}
\value{
A named list of per-coverage credible-set tables.
}
\description{
Build the per-coverage, purity-filtered credible-set tables from a SuSiE /
fSuSiE fit and its design matrix. These tables are the \code{csTables} input
to \code{\link{buildTopLoci}}.
}
\examples{
data(eqtlRegionExample)
X <- eqtlRegionExample$X[, 1:40]
y <- eqtlRegionExample$yRes
fit <- susieR::susie(X, y, L = 5)
computeCsTables(fit, dataX = X, method = "susie")
}
