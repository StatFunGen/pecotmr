% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allMethods.R
\name{QtlFineMappingResult}
\alias{QtlFineMappingResult}
\title{Create a QtlFineMappingResult Collection}
\usage{
QtlFineMappingResult(study, context, trait, method, entry, ldSketch = NULL)
}
\arguments{
\item{study}{Character vector of study identifiers (per tuple).}

\item{context}{Character vector of context labels (per tuple).}

\item{trait}{Character vector of trait identifiers (per tuple).}

\item{method}{Character vector of fine-mapping method names (per tuple).}

\item{entry}{List / \code{SimpleList} of \code{FineMappingEntry} objects.}

\item{ldSketch}{An optional \code{GenotypeHandle} (the LD reference for
RSS-derived fits), or \code{NULL} for individual-level fits.}
}
\value{
A \code{QtlFineMappingResult} object.
}
\description{
Construct a \code{QtlFineMappingResult} DFrame-subclass
  collection from per-tuple vectors and a list of
  \code{FineMappingEntry} payloads (one per tuple). The optional
  \code{ldSketch} slot records the LD reference used for RSS-derived
  fits; pass \code{NULL} (the default) for individual-level fits.
}
