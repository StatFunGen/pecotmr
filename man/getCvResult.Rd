% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/QtlFineMappingResult.R,
%   R/fineMappingRow.R, R/twasWeights.R, R/twasWeightsRow.R
\name{getCvResult}
\alias{getCvResult}
\alias{getCvResult,QtlFineMappingResult-method}
\alias{getCvResult,FineMappingRow-method}
\alias{getCvResult,TwasWeights-method}
\alias{getCvResult,TwasWeightsRow-method}
\title{Get Cross-Validation Result}
\usage{
getCvResult(x, ...)

\S4method{getCvResult}{QtlFineMappingResult}(x, study = NULL, context = NULL, trait = NULL, method = NULL, ...)

\S4method{getCvResult}{FineMappingRow}(x, ...)

\S4method{getCvResult}{TwasWeights}(x, study = NULL, context = NULL, trait = NULL, method = NULL, ...)

\S4method{getCvResult}{TwasWeightsRow}(x, ...)
}
\arguments{
\item{x}{A \code{FineMappingRow} or \code{FineMappingResult}.}

\item{...}{Class-specific selection arguments.}

\item{study}{Character (length 1) or \code{NULL}. Restrict the selection to
this study; \code{NULL} matches all studies.}

\item{context}{Character (length 1) or \code{NULL}. Restrict the selection to
this context; \code{NULL} matches all contexts.}

\item{trait}{Character (length 1) or \code{NULL}. Restrict the selection to
this trait; \code{NULL} matches all traits.}

\item{method}{Character (length 1) or \code{NULL}. Restrict the selection to
this fine-mapping / weight method; \code{NULL} matches all methods.}
}
\value{
A list (the CV payload) or \code{NULL}.
}
\description{
Extract the cross-validation payload stored on a
  \code{FineMappingRow} (or the matching entry of a
  \code{FineMappingResult}). The payload is a list with components
  \code{samplePartition} (a \code{data.frame} of \code{Sample}/\code{Fold}
  assignments), \code{predictions} (a named list of per-method out-of-fold
  prediction matrices), and \code{performance} (a named list of per-method
  metric matrices). \code{NULL} when fine-mapping was run without
  cross-validation (\code{cvFolds <= 1}).
}
\examples{
data(qtlFineMappingExample)
getCvResult(qtlFineMappingExample)
}
