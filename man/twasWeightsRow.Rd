% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twasWeightsRow.R
\name{twasWeightsRow}
\alias{twasWeightsRow}
\title{Build One TWAS-Weight Row}
\usage{
twasWeightsRow(
  variantIds,
  weights,
  fits = NULL,
  cvResult = NULL,
  standardized = FALSE,
  dataType = NULL
)
}
\arguments{
\item{variantIds}{Character vector of variant ids, each encoding
coordinates (\code{chrom:pos:ref:alt}).}

\item{weights}{Per-variant weights, aligned to \code{variantIds} (a vector,
or a matrix with one column per condition).}

\item{fits}{Optional per-method fit payload.}

\item{cvResult}{Optional cross-validation payload.}

\item{standardized}{Whether the weights are already on the standardized
scale.}

\item{dataType}{Optional data-type label.}
}
\value{
A \code{\linkS4class{TwasWeightsRow}}.
}
\description{
Assemble a single row's payload for
  \code{\link{TwasWeights}}: the variants, their weights and the fit
  payload. Pass a list of these as the collection's \code{entry} argument.
}
\examples{
row <- twasWeightsRow(
    variantIds = c("chr1:100:A:G", "chr1:200:C:T"),
    weights = c(0.4, -0.2)
)
TwasWeights(
    study = "s1", context = "brain", trait = "g1", method = "lasso",
    entry = list(row)
)
}
\seealso{
\code{\link{fineMappingRow}}
}
